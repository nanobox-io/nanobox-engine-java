#!/bin/bash
# -*- mode: bash; tab-width: 2; -*-
# vim: ts=2 sw=2 ft=bash noet

# $1 = code_dir

[ -f ${1}/pom.xml ] && exit 0

# gradle.org (supports c builds, but seems to be mainly for java)
[ -f ${1}/build.gradle ] && exit 0
exit 1
